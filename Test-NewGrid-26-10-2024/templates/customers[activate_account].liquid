{% layout settings.customer_layout %}
<div id="customer-activate" class="row">
  <div class="col-md-12">
    <h1>Kích hoạt tài khoản</h1>
    <p class="note create-password">Điền mật khẩu để kích hoạt tài khoản của bạn</p>

    {% form 'activate_customer_password' %}
    {{ form.errors | default_errors }}

    <label class="label" for="password">Mật Khẩu</label>
    <input type="password" value="" name="customer[password]" id="customer_password" class="text" size="16" />

    <label class="label" for="password_confirmation">Xác nhận mật khẩu</label>
    <input type="password" value="" name="customer[password_confirmation]" id="customer_password_confirmation" class="text" size="16" />

    <div class="action_bottom">
      <input class="btn" type="submit" value="Kích hoạt" />
      <span class="note">hoặc</span>
      <a href="/">Từ chối đăng ký</a>
    </div>
    {% endform %}
  </div> 
</div>